#pragma once

// File with application configuration (made for evading circular import)
namespace config
{
	// Window constraints
	extern const unsigned int WINDOW_SIZE_X, WINDOW_SIZE_Y;
	extern const float ASPECT;
}
